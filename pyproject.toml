[project]
name = "iot-esp8266-fairy-lights"
version = "0.1.0"
description = "ESP8266 firmware project built with PlatformIO"
requires-python = ">=3.10.0"
dependencies = [
    "platformio>=6.1.0"
]

[tool.uv]
package = false
